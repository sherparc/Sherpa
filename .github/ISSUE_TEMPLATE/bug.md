---
name: Bug
about: Something Sherpa does that it should not, or a traceback
labels: bug
---

**Version and environment** — the output of `sherpa doctor --json` (nothing private in it: versions, paths
inside the repository, the trunk).

**The command** — exactly as typed, with its output.

**What you expected** instead.

**A way to reproduce it** on a small fixture repository — the fixtures in `tests/conftest.py` show the shape.
Never paste a private repository's contents.
