.venv/
__pycache__/
*.pyc
dist/
build/
*.egg-info/
.pytest_cache/
.idea/
# sherpa on itself: plan and state are checked in (ADR-0005), the model is regenerated, telemetry ignores itself
.sherpa/codebase-model.json
tests/corpus/
.coverage

# test/lint caches and reports
htmlcov/
coverage.xml
.mypy_cache/
.ruff_cache/
# local Claude Code settings and memory (shared harness files live in .claude/ and are tracked)
.claude/settings.local.json
.claude/memory/
# Obsidian projection of the docs (scripts/sync-kb.py) — personal, regenerated, never a source
kb-sherpa/
# secrets / environment
.env
.env.*
# OS / editors
.DS_Store
Thumbs.db
.vscode/
