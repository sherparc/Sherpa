---
description: Sync the Obsidian vault kb-sherpa/ from the repository (one way) and report what changed.
---

Run `python3 scripts/sync-kb.py` from the repository root and report the summary line. If it fails, show the
error and stop. Never edit files under `kb-sherpa/` — they are a projection (see `.claude/skills/sync-kb/SKILL.md`).
