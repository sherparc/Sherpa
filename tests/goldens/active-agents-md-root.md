# shop

<!-- sherpa:begin harness -->
## AI harness (managed by sherpa)

Module facts live in `.agents/docs/modules/` (one owner doc per module, the single place for a
fact); procedures in `.agents/skills/`. Blocks between `sherpa:begin` and `sherpa:end` markers
are regenerated by `sherpa apply` — write outside them. Integrity: `sherpa status` or
`python3 .agents/scripts/sherpa-check.py`.

Nearest AGENTS.md per module (loaded when you work there), most active first:

- `svc/pay/AGENTS.md` — pay
- `svc/core/AGENTS.md` — core
- `tests/suite/AGENTS.md` — suite
<!-- sherpa:end harness -->
