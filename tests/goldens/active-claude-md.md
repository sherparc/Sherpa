# shop

@AGENTS.md

<!-- sherpa:begin harness -->
## AI harness (managed by sherpa)

Module facts live in `.agents/docs/modules/` — one owner doc per module, the single place
for a fact. Agents in `.claude/agents/` carry a role and a knowledge manifest, never facts;
skills in `.agents/skills/` are procedures. Blocks between `sherpa:begin` and `sherpa:end`
markers are regenerated by `sherpa apply` — write outside them. Integrity: `sherpa status`
or `python3 .agents/scripts/sherpa-check.py`.
<!-- sherpa:end harness -->
